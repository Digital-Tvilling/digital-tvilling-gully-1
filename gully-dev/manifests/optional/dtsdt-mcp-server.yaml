apiVersion: apps/v1
kind: Deployment
metadata:
  name: dtsdt-mcp-server
  namespace: ${NAMESPACE}
  annotations:
    keel.sh/policy: "force"
    keel.sh/trigger: "poll"
    keel.sh/pollSchedule: "@every 5m"
    keel.sh/match-tag: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dtsdt-mcp-server
  template:
    metadata:
      labels:
        app: dtsdt-mcp-server
    spec:
      imagePullSecrets:
        - name: dockerhub
      containers:
        - name: dtsdt-mcp-server
          image: digitaltvilling/dtsdt-mcp-server:${IMAGE_TAG_SUFFIX}
          imagePullPolicy: Always
          ports:
            - containerPort: 3052
          env:
            - name: TRANSPORT
              value: "http"
            - name: MCP_PORT
              value: "3052"
            - name: NEO4J_URI
              value: "${DTSDT_NEO4J_URI}"
            - name: NEO4J_USER
              value: "${DTSDT_NEO4J_USER}"
            - name: NEO4J_PASSWORD
              value: "${DTSDT_NEO4J_PASSWORD}"
            - name: NEO4J_DATABASE
              value: "${DTSDT_NEO4J_DATABASE}"
          livenessProbe:
            tcpSocket:
              port: 3052
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            tcpSocket:
              port: 3052
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: dtsdt-mcp-server
  namespace: ${NAMESPACE}
spec:
  selector:
    app: dtsdt-mcp-server
  ports:
    - port: 80
      targetPort: 3052
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dtsdt-mcp-server
  namespace: ${NAMESPACE}
spec:
  rules:
    - host: dtsdt-mcp.${DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dtsdt-mcp-server
                port:
                  number: 80
